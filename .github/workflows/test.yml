name: test workflow

on:
  workflow_dispatch:

jobs:
  job1:
    steps:
      - run: echo "job1"
  job2:
     steps:
       - run: echo "job2"  
    needs: 
      - job1
  job3:
    steps:
      - run: echo "job3"
    needs: [job1, job2]
  
    
 
